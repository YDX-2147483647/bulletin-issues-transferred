{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "ding.secrets.schema.json-v0.0.1",
    "type": "object",
    "description": "钉钉→设置→机器人管理→任意机器人的设置",
    "properties": {
        "webhook": {
            "type": "string",
            "description": "Webhook，“https://oapi.dingtalk.com”开头"
        },
        "secret": {
            "type":"string",
            "description": "安全设置→加签，“SEC”开头"
        }
    },
    "required": [
        "webhook",
        "secret"
    ]
}